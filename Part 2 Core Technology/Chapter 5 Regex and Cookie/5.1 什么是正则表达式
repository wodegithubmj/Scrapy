5.1 什么是正则表达式
    正则表达式就是描述字符串排列的一套规则。
    在Python中，一般使用re模块实现Python正则表达式的功能。
    
5.2 正则表达式基础知识
    正则表达式的原子、元字符、模式修正符、贪婪模式、懒惰模式。
    
    1.原子
      原子是正则表达式中最基本的单位。常见原子如下：
      1）普通字符作为原子
      2）非打印字符作为原子
      3）通用字符作为原子
      4）原子表
      
      （1）普通字符作为原子
           数字、大小写字母、下划线等都可以作为原子使用。
           
      （2）非打印字符作为原子
           非打印字符，指的是一些在字符串中用于格式控制的符号，比如换行符“\n”和制表符“\t”。
           
      （3）通用字符作为原子
           指一个原子可以匹配一类字符。比如“\w”匹配任意一个字母、数字或下划线，“\W”匹配除字母、数字和下划线以外的任意一个字符。
           “\d”匹配任意一个十进制数。“\D”匹配除十进制数以外的任意一个其他字符。
           “\s”匹配任意一个空白字符。“\S”匹配除空白字符以外的任意一个其他字符。
           
      （4）原子表
           原子表可以定义一组地位平等的原子，然后匹配时会取该原子表中任意一个原子进行匹配，比如[xyz]。
      
